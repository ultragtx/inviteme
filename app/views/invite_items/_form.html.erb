<div class="login-form">
<%= simple_form_for @invite_item do |f| %>
<% if @invite_item.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@invite_item.errors.count, "error") %> prohibited this invite_item from being saved:</h2>

    <ul>
    <% @invite_item.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>
<%= f.input :title %>
<%= f.input :url %>
<%= f.input :description %>
<%= f.input :codes_str %>
<%= f.input :public %>
<%= f.button :submit %>
<%end%>
</div>