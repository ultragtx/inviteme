<div class="login-form">
<h2>Take a code</h2>

<!-- <div>Title:</div>
<div><%= @current_invite_item.title %></div>
<div>URL:</div>
<div><%= auto_link(@current_invite_item.url) %></div>
<div>Description:</div>
<div><%= @current_invite_item.description %></div> -->

<table class="table table-bordered table-striped">
  <tbody>
    <tr>
      <td>Title</td>
      <td><%= @current_invite_item.title %></td>
    </tr>
    <tr>
      <td>URL</td>
      <td><%= auto_link("@current_invite_item.url") %></td>
    </tr>
	<% if @current_invite_item.description && @current_invite_item.description.length > 0%>
    <tr>
      <td>Desc</td>
      <td><%= @current_invite_item.description %></td>
    </tr>
	<% end %>
  </tbody>
</table>

<!-- <div class="input-append">
	<input class="span2" id="appendedInputButton" type="text">
	<%= button_to 'Take One', invite_code_path(@current_invite_item), remote: true, method: 'put', class: "btn", type: "button" %>
</div> -->

<%= form_tag({controller: '/invite_codes', action: 'update', id: @current_invite_item.id}, {method: 'put', remote: true} ) do %>
<div class="input-append">
  <input class="span2" id="codeinput" type="text" placeholder="Press the button to get a code" readonly>
  <%= submit_tag "Take One", class: "btn", id: "codebutton" %>
</div>

<% end %>

</div>